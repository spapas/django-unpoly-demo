services:
  - type: web
    name: unpoly_demo
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn unpoly_demo.wsgi:application"
    envVars:
      - key: SECRET_KEY
        generateValue: true
